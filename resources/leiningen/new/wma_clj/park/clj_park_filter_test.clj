(ns {{name}}.filter-test
    (:require [{{name}}.filter :as subject]
              [clojure.test :refer :all]))

(deftest filter-test
  (testing "exec"
    (is (= 0 1)))
  (testing "wire"
    (is (= 0 1))))
